# 23 — Partitive Hyperedge (closed enumeration, no markers)
#
# A plain closed partitive decomposition: the comprehensive concept
# is composed of EXACTLY the listed parts, with NO plurality
# notation (no diagram-notation flags to carry). This is the minimal
# form of a hyperedge: comprehensive + parts + (implicit closed)
# enumeration.
#
# Distinct from example 20 (which carries a `double` marker) and
# example 21 (which uses `enumeration: open`). Use this shape when
# the source has no VIM-style rake notation and the relationship is
# simply "these are the parts, all of them."

---
id: "113-01-01"
data:
  identifier: "113-01-01"
  localized_concepts:
    eng: 33333333-aaaa-bbbb-cccc-000000000001
  domains:
    - concept_id: section-113-1
      source: "EXAMPLE"
      ref_type: section

related: []

partitive_hyperedges:
  - comprehensive:
      source: "EXAMPLE"
      id: "113-01-01"
    parts:
      - source: "EXAMPLE"
        id: "113-01-02"
      - source: "EXAMPLE"
        id: "113-01-03"
    # enumeration omitted → defaults to "closed" via schema default
    content: |
      A simple partitive decomposition: the comprehensive is composed
      of exactly two parts, no plurality markers needed.

status: valid
date_accepted: "2026-07-01T00:00:00+00:00"
---
data:
  dates:
    - date: "2026-07-01"
      type: accepted
  definition:
    - content: "example comprehensive concept"
  terms:
    - type: expression
      normative_status: preferred
      designation: "example comprehensive"
  language_code: eng
  entry_status: valid
