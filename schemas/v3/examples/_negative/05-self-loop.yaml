# 05 — Self-loop: comprehensive also appears in parts.
#
# EXPECT_PASS — the JSON schema does NOT currently enforce no-self-loop.
# The SHACL warning shape (PartitiveHyperedgeOwnershipShape) catches a
# related invariant at the RDF layer. This negative example documents
# the gap — it passes schema validation today. Tracked in
# TODO.complete/14 and TODO.hyperedges-v2/09.
id: "900-05-01"
data:
  identifier: "900-05-01"
partitive_hyperedges:
  - comprehensive:
      source: "X"
      id: "900-05-01"
    parts:
      - source: "X"
        id: "900-05-01"
    enumeration: closed
status: valid
